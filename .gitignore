# run scripts (contain secrets)
run.sh
run.ps1

# local env (real vault path + password) — keep .env.example tracked
.env

# editor / local tooling config
.claude/

# binaries
/bookwatch
*.exe

# local SQLite db
*.db
*.db-wal
*.db-shm
